// Fallback { bg, border, text } color tokens for any dropdown value that
// doesn't have a dedicated entry in its module's color map.
export const COLOR_FALLBACK = { bg: "#f1f5f9", border: "#94a3b8", text: "#475569" };
